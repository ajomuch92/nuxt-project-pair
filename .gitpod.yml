# Commands to start on workspace startup
tasks:
  - name: Start server
    init: npm install
    command: npm run dev
# Ports to expose on workspace startup
ports:
  - port: 3000
    onOpen: open-browser